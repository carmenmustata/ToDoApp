class ToDo {
  String? id;
  String? todoText;
  bool isDone;

  ToDo({
    required this.id,
    required this.todoText,
    this.isDone = false,
  });

  static List<ToDo> todoList() {
    return [
      ToDo(id: '01', todoText: 'aaaaa', isDone: true),
      ToDo(id: '02', todoText: 'Make Weekly presentation', isDone: true),
      ToDo(id: '03', todoText: 'Follow a Flutter tutorial.', isDone: true),
      ToDo(
        id: '04',
        todoText: 'Create a To-do app.',
        isDone: true,
      ),
      ToDo(
        id: '05',
        todoText: 'Make research about Kafka.',
      ),
      ToDo(
        id: '06',
        todoText: 'Configure a Libelium Station.',
      ),
      ToDo(
        id: '07',
        todoText: 'Finish AI courses.',
      ),
    ];
  }
}
